//Start of React native CLI
import { AppRegistry } from 'react-native'
import App from './App'
import { name as appName } from './app.json'

AppRegistry.registerComponent(appName, () => App)


//This allows you to optionally have multiple React Native root components,
// which you might embed into different native screens.
// End of React native CLI 
